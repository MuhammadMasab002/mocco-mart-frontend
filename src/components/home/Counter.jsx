import React from "react";
import CustomButton from "../common/CustomButton";

const Counter = () => {
  return (
    <div className="card">
      <CustomButton />
    </div>
  );
};

export default Counter;
